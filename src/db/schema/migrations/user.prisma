model User {
    id: Int     @id @default(autoincrement())
    firstName: String
    lastName: String
    email: String @unique
    password: String
    isAdmin: Bool @default(false)

    @@map("Users")
}