model SkinType {
  id               Int                @id @default(autoincrement())
  name             String
  description      String
  ingredientEffect IngredientEffect[]
  userSkinType     UserSkinType[]
}
